@model List<NewNew.Models.Domain.Fine>

<h1>All Fined Books</h1>

<div class="card mb-4 mt-4">
    <div class="card-header">
        <i class="fas fa-table me-1"></i>
        All Fined Books
        <a href="@Url.Action("Index", "Home")" class="float-end btn btn-info">--Home</a>

    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table id="datatablesSimple" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Book Title</th>
                        <th>Fine Amount</th>
                        <th>Fine Date</th>
                      
                      @*  <th>Action</th>*@
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>#</th>
                        <th>Book Title</th>
                        <th>Fine Amount</th>
                        <th>Fine Date</th>
                       
                       @* <th>Action</th>*@
                    </tr>
                </tfoot>
                <tbody>
                    @{
                        int counter = 1;
                    }
                    @foreach (var fine in Model)
                    {
                        <tr>
                            <td>@counter</td>
                            <td>@fine.bookTitle</td>
                            <td>@fine.FineAmount birr</td>
                            <td>@fine.FineDate</td>
                          
                            @*  <td><a href="@Url.Action("View", "Fine", new { id = fine.FineId})" class="btn btn-info">View</a></td>
                        *@
                        </tr>
                        counter++;
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
<script src="~/js/scripts.js"></script>
<script src="https://cdn.jsdelivr.net/npm/simple-datatables@latest" crossorigin="anonymous"></script>
<script src="~/js/datatables-simple-demo.js"></script>
